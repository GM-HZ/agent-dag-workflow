interface:
  display_name: "Agent Workflow Builder"
  short_description: "Discover, run, and build durable DAG workflows"
  default_prompt: "Use $workflow-builder to find or build and safely run a durable Agent DAG workflow."
